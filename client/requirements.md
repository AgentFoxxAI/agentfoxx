## Packages
recharts | Dashboard analytics charts and data visualization
date-fns | Human-readable date formatting for activities
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
- Audio recording requires browser microphone permissions (navigator.mediaDevices.getUserMedia).
- POST /api/upload-audio requires multipart/form-data, not JSON. The frontend must manually construct this payload.
